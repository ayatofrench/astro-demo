---
const rand = Math.floor(Math.random() * 200);
const response = await fetch(`https://pokeapi.co/api/v2/pokemon/${rand}`);
const data = await response.json();
const pokemon = data;
---

<div>
  This is an astro tab!!
</div>

<div>
  Who is that pokemon!?!: <i>{pokemon.species.name}</i>
</div>
